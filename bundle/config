---
BUNDLE_PATH: ".bundle"
BUNDLE_JOBS: '3'
BUNDLE_BUNDLE_WITHOUT: 'production # for eventmachine: Fix SSL problem now that OSX
  10.11+ doesn''t really care about OpenSSL'
BUNDLE_BUILD__EVENTMACHINE: "--with-cppflags=-I/usr/local/opt/openssl/include"
BUNDLE_BUILD__PUMA: "--with-opt-dir=/usr/local/opt/openssl"
